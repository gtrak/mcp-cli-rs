---
phase: 15-documentation-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli/call.rs
  - src/ipc/mod.rs
  - src/ipc/windows.rs
  - src/parallel.rs

must_haves:
  truths:
    - "Running `cargo doc` produces zero warnings"
    - "All HTML tags in documentation are properly closed"
    - "All URLs in documentation are valid hyperlinks"
  artifacts:
    - path: "src/cli/call.rs"
      provides: "Fixed unclosed HTML tag for Mutex"
      contains: "`Arc<Mutex>`"
    - path: "src/ipc/mod.rs"
      provides: "Fixed unclosed HTML tags for dyn"
      contains: "`dyn`"
    - path: "src/ipc/windows.rs"
      provides: "Fixed bare URLs to hyperlinks"
      contains: "<https://"
    - path: "src/parallel.rs"
      provides: "Fixed unclosed HTML tags for ToolInfo and String"
      contains: "properly closed tags"
---

<objective>
Fix all cargo doc warnings to achieve zero warnings (DOC-01, DOC-06).

Purpose: Clean documentation build is a prerequisite for accurate API documentation.
Output: All 8 doc warnings resolved, `cargo doc` runs clean.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md (DOC-01, DOC-06)
@.planning/ROADMAP.md (Phase 15: Documentation & API)
@.planning/STATE.md (Phase 14 complete, Phase 15 pending)
</context>

<tasks>

<task type="auto">
  <name>Fix HTML tag warnings in call.rs, mod.rs, parallel.rs</name>
  <files>src/cli/call.rs, src/ipc/mod.rs, src/parallel.rs</files>
  <action>
Fix unclosed HTML tags in documentation:
1. src/cli/call.rs line 23: Change `Arc<Mutex>` to `\`Arc<Mutex>\`` (backticks for code)
2. src/ipc/mod.rs lines 62, 234, 246: Change `dyn` to `\`dyn\`` in documentation
3. src/parallel.rs line 123: Change `ToolInfo` to `\`ToolInfo>\`` (check context)
4. Find and fix any other unclosed `String` tag

Use backticks to mark source code in documentation (e.g., `type`, `dyn`, `String`).
  </action>
  <verify>Run `cargo doc 2>&1 | grep -E "warning:"` - should return no warnings</verify>
  <done>`cargo doc` produces zero warnings</done>
</task>

<task type="auto">
  <name>Fix bare URL warnings in ipc/windows.rs</name>
  <files>src/ipc/windows.rs</files>
  <action>
Fix bare URLs to be valid hyperlinks:
1. Line 54: Change `https://docs.rs/...` to `<https://docs.rs/...>` (angle brackets)
2. Line 55: Change `https://learn.microsoft.com/...` to `<https://learn.microsoft.com/...>`

Use angle bracket syntax for automatic link conversion.
  </action>
  <verify>Run `cargo doc 2>&1 | grep -E "warning:|bare_urls"` - should show no bare URL warnings</verify>
  <done>No bare URL warnings in cargo doc output</done>
</task>

</tasks>

<verification>
Run `cargo doc 2>&1` - verify zero warnings in output.
</verification>

<success_criteria>
- [ ] `cargo doc` produces zero warnings
- [ ] All HTML tags properly closed with backticks
- [ ] All URLs converted to hyperlinks with angle brackets
</success_criteria>

<output>
After completion, create `.planning/phases/15-documentation-api/15-01-SUMMARY.md`
</output>
