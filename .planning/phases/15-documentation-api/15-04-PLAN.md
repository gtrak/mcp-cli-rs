---
phase: 15-documentation-api
plan: 04
type: execute
wave: 2
depends_on: [15-01, 15-02, 15-03]
files_modified: []
must_haves:
  truths:
    - "`cargo doc` generates zero warnings"
    - "Public API surface is reduced by 50-100 lines"
    - "All public modules and functions are documented"
  artifacts:
    - path: "verification results"
      provides: "DOC-01 through DOC-06 compliance proof"
---

<objective>
Final verification that all Phase 15 documentation requirements are met.

Purpose: Ensure all documentation work is complete and compliant.
Output: Verified zero warnings, documented API.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md (DOC-01 through DOC-06, SIZE-05)
@.planning/ROADMAP.md (Phase 15)
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Verify zero doc warnings</name>
  <files>src/**/*.rs</files>
  <action>
Final verification steps:
1. Run `cargo doc 2>&1` - must show zero warnings
2. Run `cargo doc --document-private-items 2>&1` - verify internal docs also clean
3. Count and report any remaining warnings

If any warnings remain, fix them in this task.
  </action>
  <verify>`cargo doc 2>&1 | grep -c warning` returns 0</verify>
  <done>Zero documentation warnings</done>
</task>

<task type="auto">
  <name>Verify API surface reduction</name>
  <files>src/lib.rs</files>
  <action>
Compare public API before/after:
1. Document current public exports (use `cargo doc --document-private-items` or review lib.rs)
2. Verify at least 50-100 lines of unnecessary exports identified in Plan 02
3. Confirm SIZE-05 target met
  </action>
  <verify>Review code changes from Plan 02</verify>
  <done>API surface reduced per SIZE-05</done>
</task>

<task type="auto">
  <name>Run all tests to verify no regressions</name>
  <files>all test files</files>
  <action>
Run full test suite to ensure documentation work didn't break anything:
1. `cargo test --lib` - library tests
2. `cargo test` - all tests including integration
3. `cargo clippy --lib` - verify no new warnings
  </action>
  <verify>All tests pass, clippy clean</verify>
  <done>All tests pass, zero clippy warnings</done>
</task>

</tasks>

<verification>
- `cargo doc` = zero warnings
- `cargo test` = all pass
- `cargo clippy --lib` = zero warnings
</verification>

<success_criteria>
- [ ] DOC-01: All doc warnings fixed
- [ ] DOC-02: Public API audited
- [ ] DOC-03: API surface reduced by 50-100 lines
- [ ] DOC-04: Module docs improved
- [ ] DOC-05: All public APIs documented
- [ ] DOC-06: Zero warnings
- [ ] SIZE-05: API surface reduced ~50-100 lines
</success_criteria>

<output>
After completion, create `.planning/phases/15-documentation-api/15-04-SUMMARY.md`
</output>
