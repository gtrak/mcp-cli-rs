---
phase: 16-code-quality-sweep
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli/models.rs

must_haves:
  truths:
    - "No unnecessary #[allow(dead_code)] attributes remain"
    - "Dead code actually removed or attribute justified"
  artifacts:
    - path: "src/cli/models.rs"
      provides: "Clean dead code handling"
      contains: "no #[allow(dead_code)]"
---

<objective>
Remove unnecessary #[allow(dead_code)] attributes (QUAL-02).

Purpose: Clean slate approach - if something is unused, remove it entirely.
Output: Zero unnecessary dead code attributes.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md (QUAL-02)
@.planning/ROADMAP.md (Phase 16)
@.planning/phases/16-code-quality-sweep/16-CONTEXT.md

**User Decision:** "Remove ALL #[allow(dead_code)] attributes - clean slate approach"
</context>

<tasks>

<task type="auto">
  <name>Review and remove dead code attributes</name>
  <files>src/cli/models.rs</files>
  <action>
Check the two #[allow(dead_code)] attributes in src/cli/models.rs (lines 187, 193):
1. Read the code around these lines to understand what's being allowed
2. If the code is truly unused, remove it entirely
3. If it has future purpose, leave it but add comment explaining why
4. Remove the #[allow(dead_code)] attribute if code is removed

From CONTEXT.md: "Clean slate approach — if something is truly unused, remove it entirely"
  </action>
  <verify>grep -n "allow(dead_code)" src/ - should return no matches</verify>
  <done>All dead code attributes removed</done>
</task>

</tasks>

<verification>
- Run `grep -r "allow(dead_code)" src/` - should return no matches
- Ensure `cargo check` still passes
</verification>

<success_criteria>
- [ ] Zero #[allow(dead_code)] attributes remain
- [ ] Code compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/16-code-quality-sweep/16-02-SUMMARY.md`
</output>
